{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read f; if echo \"$f\" | grep -qE '[.](m?[jt]sx?|json|md)$'; then bunx --bun biome check --write --log-level error \"$f\"; fi; }"
          }
        ]
      }
    ]
  }
}
